// import { Page } from 'landingPage/core/utils';
import { defaultTheme, styled } from 'landingPage/globalStyles/theme';

export const Layout = styled.main`
  position: 'static';
  background-color: ${defaultTheme.colors.baseWhiteFont};
`;
