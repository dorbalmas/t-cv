import { motion } from 'framer-motion';

import { styled } from '@/components/landingPage/globalStyles/theme';

export const StyledElement = styled(motion.div)`
  will-change: transform, opacity, scale;
`;
